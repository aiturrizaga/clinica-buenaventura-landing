# eslint-plugin-astro exige eslint>=10 y eslint-plugin-jsx-a11y solo declara soporte hasta eslint@9:
# los dos conflictos de peer deps son solo de declaración (ambos funcionan), pero npm los trata como ERESOLVE.
legacy-peer-deps=true

---
interface Props {
    image: string;
    alt?: string;
    waMessage?: string;
}

const {
    image,
    alt = 'Promoción Clínica Buenaventura',
    waMessage,
} = Astro.props;

function waLink(text: string) {
    return `https://wa.me/51932939530?text=${ encodeURIComponent(text) }`;
}
---

<!-- Overlay -->
<div id="promo-overlay" style="opacity: 0"
     class="fixed inset-0 z-200 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm">

    <!-- Modal -->
    <div class="relative w-full max-w-lg md:max-w-3xl lg:max-w-5xl rounded-2xl overflow-hidden shadow-2xl">

        <!-- Close button -->
        <button
                id="promo-close"
                aria-label="Cerrar promoción"
                class="absolute top-3 right-3 z-10 size-10 cursor-pointer rounded-full bg-white text-gray-800 border border-gray-300 flex items-center justify-center shadow-lg hover:bg-gray-100 transition-colors duration-200"
        >
            <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"
                 aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
            </svg>
        </button>

        <!-- Imagen -->
        { waMessage ? (
                <a href={ waLink(waMessage) } target="_blank" rel="noopener noreferrer">
                    <img
                            src={ image }
                            alt={ alt }
                            class="w-full h-auto block"
                            loading="eager"
                    />
                </a>
        ) : (
                <img
                        src={ image }
                        alt={ alt }
                        class="w-full h-auto block"
                        loading="eager"
                />
        ) }
    </div>
</div>

<script>
    const overlay = document.getElementById('promo-overlay')!;
    const closeBtn = document.getElementById('promo-close')!;

    function close() {
        sessionStorage.setItem('promo-seen', '1');
        overlay.style.opacity = '0';
        setTimeout(() => overlay.remove(), 350);
    }

    closeBtn.addEventListener('click', close);
    overlay.addEventListener('click', (e) => {
        if (e.target === overlay) close();
    });
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') close();
    });

    // Entrada con delay
    if (!sessionStorage.getItem('promo-seen')) {
        overlay.style.opacity = '0';
        overlay.style.transition = 'opacity 0.35s ease';
        setTimeout(() => {
            overlay.style.opacity = '1';
        }, 600);
    } else {
        overlay.remove();
    }
</script>

---
import type { Specialty } from './types';

interface Props {
    specialty: Pick<Specialty, 'name' | 'fontIcon'>;
    /** Muestra un chevron para indicar que la tarjeta despliega opciones. */
    expandable?: boolean;
}
const { specialty, expandable = false } = Astro.props;
---
<div class="w-14 h-14 rounded-xl bg-primary-50 text-primary flex items-center justify-center shrink-0 group-hover:bg-primary group-hover:text-white transition-colors duration-200">
    <i class={ `ti ti-${ specialty.fontIcon }` }
       style="font-size: 26px;"
       aria-hidden="true"
    ></i>
</div>

<h3 class="text-sm font-bold text-gray-900 m-0 leading-tight group-hover:text-white transition-colors duration-200">
    { specialty.name }
</h3>

{ expandable && (
        <i class="ti ti-chevron-down text-xs text-gray-400 transition-transform duration-200 group-open/menu:rotate-180"
           aria-hidden="true"></i>
) }
